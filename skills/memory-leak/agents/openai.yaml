interface:
  display_name: "Flutter Memory Leak"
  short_description: "Audit Flutter memory leak risks"
  default_prompt: "Use $memory-leak to review Flutter changes for high-confidence memory leak violations only."

policy:
  allow_implicit_invocation: true
